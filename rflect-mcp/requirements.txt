# RFlect MCP Server Dependencies

# MCP SDK (FastMCP). 2.x dropped mcp.server.fastmcp and breaks this server.
mcp>=1.0.0,<2.0.0

# RFlect core (install from parent directory)
# pip install -e ..

# Report generation
python-docx>=1.2.0

# Template engine (rflect-mcp/templates/default.yaml)
pyyaml>=6.0
